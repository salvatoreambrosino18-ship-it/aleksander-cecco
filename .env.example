# Aleksander Cecco - environment variables
# Copy to .env and fill in real values. Never commit .env.
# Variable names only here, no values.

# --- Sanity (projectId and dataset are public identifiers, not secrets) ---
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=
PUBLIC_SANITY_API_VERSION=
# Server-only read token, needed only for private datasets or draft/preview reads.
SANITY_READ_TOKEN=

# --- Site ---
# Absolute site URL, used for canonical links, Open Graph, and the sitemap.
PUBLIC_SITE_URL=https://aleksandercecco.com

# --- Enquiry email (Resend) ---
# These are SERVER secrets for the Cloudflare Pages Function. They belong in the
# Pages project settings, not in the build environment, and never in git.
# Without all three the form validates, refuses honestly, and sends nothing.
RESEND_API_KEY=
# The FROM address. Until a domain is bought and verified in Resend, the only
# address available is onboarding@resend.dev, and Resend will then deliver ONLY
# to the address that owns the Resend account. See DESIGN-PLAN section 19.
RESEND_FROM=
# Where enquiries are delivered. Until the domain exists this must be the email
# the Resend account was opened with.
ENQUIRY_TO_EMAIL=

# --- Search visibility ---
# "true" ONLY at launch. Any other value (or unset) keeps every page noindex.
# The second lock is the X-Robots-Tag header in public/_headers, removed by hand.
PUBLIC_ALLOW_INDEXING=

# --- Analytics (Cloudflare Web Analytics, cookieless) ---
PUBLIC_CF_WEB_ANALYTICS_TOKEN=

# --- Deploy (used by the Sanity webhook, not by the app at runtime) ---
# Cloudflare Pages deploy hook that a Sanity content-publish webhook calls.
CLOUDFLARE_DEPLOY_HOOK_URL=
