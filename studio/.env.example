# Sanity Studio environment (studio/.env)
# Copy to studio/.env and fill in. Both values are public identifiers, not secrets.
# The studio will not boot until PROJECT_ID is set.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
